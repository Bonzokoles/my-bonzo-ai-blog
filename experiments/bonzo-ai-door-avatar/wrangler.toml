name = "bonzo-ai-door-avatar"
main = "src/workers/ai-avatar.ts"
compatibility_date = "2024-10-30"
compatibility_flags = ["nodejs_compat"]

# Account ID will be set via CLOUDFLARE_ACCOUNT_ID env var
# OPENAI_API_KEY will be set via environment variable

[vars]
# These will be overridden by environment variables in production
CF_ACCOUNT_ID = "your-cloudflare-account-id"
