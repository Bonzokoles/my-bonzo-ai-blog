{
    "scripts": {
        "dev": "astro dev",
        "dev:local": "astro dev --host 0.0.0.0 --port 4321",
        "dev:wrangler": "wrangler pages dev dist --compatibility-date=2024-10-28 --port 8787",
        "build": "astro build",
        "build:local": "astro build && echo 'Local build complete'",
        "preview": "astro preview",
        "deploy": "astro build && wrangler pages deploy dist",
        "deploy:production": "astro build && wrangler pages deploy dist --project-name=mybonzoaiblog",
        "test:local": "npm run build:local && npm run preview",
        "test:cloudflare": "npm run build && npm run dev:wrangler"
    },
    "devDependencies": {
        "@types/node": "^20.0.0"
    }
}