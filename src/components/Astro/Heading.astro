---
/* Component for rendering consistent page headings with optional background.
 *
 * Props:
 * - text?: string - Heading text content (or use slot)
 * - level?: 1-6 - Heading level (h1-h6), defaults to 1
 * - showHeadingBackground?: boolean - Enable background styling
 * - class?: string - Additional CSS classes
 *
 * Example usage:
 * <Heading
 *   text="Our Services"
 *   level={2}
 *   showHeadingBackground={true}
 *   class="my-heading"
 * />
 * OR
 * <Heading level={3}>Our Services</Heading>
 *
 * Notes:
 * - Centers text with responsive margins
 * - Optional background styling
 * - Capitalizes text by default
 * - Theme-based colors
 * - Responsive padding
 */

interface Props {
	// Text content of the heading //
	text?: string;
	// Heading level (h1-h6) //
	level?: 1 | 2 | 3 | 4 | 5 | 6;
	// Controls whether heading has background styling //
	showHeadingBackground?: boolean;
	// Optional CSS classes //
	class?: string | undefined;
}

const { text, level = 1, showHeadingBackground = true, class: className } = Astro.props;
const Tag = `h${level}` as any;
---

<div
	class:list={[
		"flex mt-12 mb-10 md:mt-24 md:mb-12",
		className,
	]}
>
	<Tag
		class:list={[
			" tracking-widest text-4xl font-medium capitalize leading-relaxed mx-auto",
			{
				"bg-theme-secondary text-theme-secondary leading-none p-4 inline-block":
					showHeadingBackground,
				"w-full": !showHeadingBackground,
			},
			{ "rounded-theme": true },
		]}
	>
		{text || <slot />}
	</Tag>
</div>
