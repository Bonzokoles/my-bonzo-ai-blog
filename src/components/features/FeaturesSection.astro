---
/**
 * Features Section Component
 * Reusable section for displaying feature cards
 */
import Card from '@components/Astro/Card.astro';
import type { FeatureCard } from '@/types/homepage';

interface Props {
  title?: string;
  description?: string;
  cards: FeatureCard[];
  className?: string;
}

const { title, description, cards, className = '' } = Astro.props;
---

<section
  class:list={[
    'mx-0 w-full flex flex-row flex-wrap-reverse justify-center gap-2 mt-4 mb-4 lg:mt-0 lg:justify-between lg:flex-nowrap lg:p-2 build-in',
    className
  ]}
>
  {cards.map((card) => (
    <Card
      title={card.title}
      description={card.description}
      shadowSize={card.shadowSize}
      iconName={card.iconName}
    />
  ))}
</section>

{title && description && (
  <section class="flex flex-col items-center justify-center mt-2 mb-8 fade-in relative z-10">
    {title && <h2 class="text-3xl md:text-4xl font-graffiti text-theme-accent mb-3">{title}</h2>}
    {description && (
      <p class="text-xl max-w-6xl leading-relaxed text-center md:max-w-screen-lg lg:text-2xl lg:leading-relaxed text-white/80">
        {description}
      </p>
    )}
  </section>
)}
